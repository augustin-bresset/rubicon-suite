# Demo environment credentials
# Copy to .env.demo and fill in real values before starting
# .env.demo is git-ignored and must never be committed

DEMO_DB_NAME=rubicondemo
DEMO_DB_USER=odoo
DEMO_DB_PASS=CHANGE_ME
