<odoo>

  <menuitem id="menu_pdp_root" name="PDP" sequence="10"/>

  <!-- 2. Sous-menu Catalogue -->
  <menuitem id="menu_pdp_catalog"
            name="Catalogue"
            parent="menu_pdp_root"
            sequence="10"/>
  <record id="action_pdp_stone_type" model="ir.actions.act_window">
    <field name="name">Types</field>
    <field name="res_model">pdp.stone.type</field>
    <field name="view_mode">tree,form</field>
    <!-- pas de <field name="view_id"> ni de <field name="views"> -->
  </record>
  <menuitem id="menu_pdp_type"
            name="Types"
            parent="menu_pdp_catalog"
            action="action_pdp_stone_type"/>
</odoo>