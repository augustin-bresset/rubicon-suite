<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">pdp.price.res.config.settings.view.form</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="90"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="PDP Price" string="PDP Price" name="pdp_price" groups="base.group_system">
                    <block title="Content Settings">
                        <setting string="Weight Unit Name" help="Display unit for weight (e.g. g, oz)">
                            <field name="pdp_weight_uom_name"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="action_pdp_price_config_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="context">{'module' : 'pdp_price'}</field>
    </record>

    <menuitem id="menu_pdp_price_settings"
        name="Configuration"
        parent="menu_root_pdp_price"
        action="action_pdp_price_config_settings"
        sequence="100"/>
</odoo>
