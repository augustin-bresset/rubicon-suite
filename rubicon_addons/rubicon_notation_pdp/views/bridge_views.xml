<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Show the PDP links in the core dictionary screens -->
    <record id="view_notation_stone_list_pdp" model="ir.ui.view">
        <field name="name">rubicon.notation.stone.list.pdp</field>
        <field name="model">rubicon.notation.stone</field>
        <field name="inherit_id" ref="rubicon_notation.view_notation_stone_list"/>
        <field name="arch" type="xml">
            <field name="name" position="after">
                <field name="type_id"/>
            </field>
        </field>
    </record>

    <record id="view_notation_shape_list_pdp" model="ir.ui.view">
        <field name="name">rubicon.notation.shape.list.pdp</field>
        <field name="model">rubicon.notation.shape</field>
        <field name="inherit_id" ref="rubicon_notation.view_notation_shape_list"/>
        <field name="arch" type="xml">
            <field name="name" position="after">
                <field name="shape_id"/>
            </field>
        </field>
    </record>

    <record id="view_notation_wizard_form_pdp" model="ir.ui.view">
        <field name="name">rubicon.notation.wizard.form.pdp</field>
        <field name="model">rubicon.notation.wizard</field>
        <field name="inherit_id" ref="rubicon_notation.view_notation_wizard_form"/>
        <field name="arch" type="xml">
            <field name="code" position="after">
                <field name="product_id" invisible="mode != 'product'"/>
            </field>
        </field>
    </record>

    <record id="view_notation_shade_map_list" model="ir.ui.view">
        <field name="name">rubicon.notation.shade.map.list</field>
        <field name="model">rubicon.notation.shade.map</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="shade_id"/>
                <field name="grade_id"/>
                <field name="hue_id"/>
            </list>
        </field>
    </record>
    <record id="action_notation_shade_map" model="ir.actions.act_window">
        <field name="name">Shade Mappings</field>
        <field name="res_model">rubicon.notation.shade.map</field>
        <field name="view_mode">list</field>
    </record>
    <menuitem id="menu_notation_shade_map" name="Shade Mappings"
              parent="rubicon_notation.menu_notation_root"
              action="action_notation_shade_map" sequence="60"/>
</odoo>
