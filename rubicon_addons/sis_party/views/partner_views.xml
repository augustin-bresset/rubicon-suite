<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- SIS Partner Tree View (standalone for SIS action) -->
    <record id="view_res_partner_tree_sis" model="ir.ui.view">
        <field name="name">res.partner.tree.sis</field>
        <field name="model">res.partner</field>
        <field name="arch" type="xml">
            <list>
                <field name="sis_code"/>
                <field name="name"/>
                <field name="company_type"/>
                <field name="parent_id" optional="hide"/>
                <field name="city"/>
                <field name="country_id"/>
                <field name="phone"/>
                <field name="email"/>
            </list>
        </field>
    </record>

    <!-- Extend Partner Form View with SIS fields -->
    <record id="view_res_partner_form_sis" model="ir.ui.view">
        <field name="name">res.partner.form.sis</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <!-- SIS Code as an explicit bold badge next to name -->
            <xpath expr="//div[hasclass('oe_title')]" position="inside">
                <h2>
                    <field name="sis_code" placeholder="SIS Code (e.g. A&amp;J)"/>
                </h2>
            </xpath>

            <!-- Additional Phones after Email -->
            <field name="email" position="after">
                <field name="sis_phone_ids" widget="one2many">
                    <list editable="bottom">
                        <field name="name" placeholder="Label"/>
                        <field name="phone" placeholder="Number"/>
                    </list>
                </field>
            </field>
            <!-- SIS tabs in notebook -->
            <xpath expr="//notebook" position="inside">
                <page string="Default" name="sis_default">
                    <group>
                        <group>
                            <field name="sis_group"/>
                            <field name="sis_contact"/>
                            <field name="sis_is_customer" widget="boolean_toggle"/>
                            <field name="sis_is_vendor" widget="boolean_toggle"/>
                        </group>
                    </group>
                    <group>
                        <group string="Sales Defaults" invisible="not sis_is_customer">
                            <field name="sis_account"/>
                            <field name="margin_id"/>
                            <field name="sis_pay_term_id"/>
                        </group>
                        <group string="Purchase Defaults" invisible="not sis_is_vendor">
                            <field name="sis_vendor_account"/>
                            <field name="sis_vendor_pay_term_id"/>
                        </group>
                    </group>
                </page>
                <page string="Shipment Info" name="sis_shipment_info">
                    <group>
                        <group string="Address">
                            <field name="sis_ship_address" string="Address"/>
                            <field name="sis_ship_city" string="City"/>
                            <field name="sis_ship_state" string="State"/>
                            <field name="sis_ship_zip" string="Zip"/>
                            <field name="sis_ship_country_id" string="Country"/>
                        </group>
                        <group string="Shipping">
                            <field name="sis_ship_method_id" string="Default Method"/>
                            <field name="sis_ship_fedex_acc" string="Fed. Ex. Acc."/>
                            <field name="sis_ship_stamp" string="Stamp"/>
                        </group>
                    </group>
                </page>
                <page string="Bank Info" name="sis_bank_info">
                    <field name="bank_ids" context="{'default_partner_id': id}">
                        <list editable="bottom">
                            <field name="sis_bank_name" string="Bank Name"/>
                            <field name="acc_holder_name" string="Acc. Name"/>
                            <field name="acc_number" string="Acc. No."/>
                            <field name="sis_bank_address" string="Bank Address"/>
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>

    <!-- SIS-specific search: filter by sis_code -->
    <record id="view_res_partner_search_sis" model="ir.ui.view">
        <field name="name">res.partner.search.sis</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter"/>
        <field name="arch" type="xml">
            <field name="name" position="after">
                <field name="sis_code"/>
            </field>
        </field>
    </record>

    <!-- SIS Parties action: show only partners with SIS code -->
    <record id="action_sis_parties" model="ir.actions.act_window">
        <field name="name">SIS Parties</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_res_partner_tree_sis"/>
        <field name="domain">[('sis_code', '!=', False)]</field>
        <field name="context">{'default_company_type': 'company'}</field>
    </record>

    <!-- PayTerm Tree -->
    <record id="view_sis_pay_term_tree" model="ir.ui.view">
        <field name="name">sis.pay.term.tree</field>
        <field name="model">sis.pay.term</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="name"/>
            </list>
        </field>
    </record>

    <record id="action_sis_pay_term" model="ir.actions.act_window">
        <field name="name">Payment Terms</field>
        <field name="res_model">sis.pay.term</field>
        <field name="view_mode">list</field>
    </record>

    <!-- Shipper Tree -->
    <record id="view_sis_shipper_tree" model="ir.ui.view">
        <field name="name">sis.shipper.tree</field>
        <field name="model">sis.shipper</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="name"/>
            </list>
        </field>
    </record>

    <record id="action_sis_shipper" model="ir.actions.act_window">
        <field name="name">Shippers</field>
        <field name="res_model">sis.shipper</field>
        <field name="view_mode">list</field>
    </record>

    <!-- Trade Fair Tree -->
    <record id="view_sis_trade_fair_tree" model="ir.ui.view">
        <field name="name">sis.trade.fair.tree</field>
        <field name="model">sis.trade.fair</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="city"/>
                <field name="country_id"/>
                <field name="date_start"/>
                <field name="date_end"/>
            </list>
        </field>
    </record>

    <record id="action_sis_trade_fair" model="ir.actions.act_window">
        <field name="name">Trade Fairs</field>
        <field name="res_model">sis.trade.fair</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
